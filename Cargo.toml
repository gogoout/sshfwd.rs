[workspace]
members = [
    "crates/sshfwd",
    "crates/sshfwd-agent",
    "crates/sshfwd-common",
]
resolver = "2"

[workspace.package]
version = "0.4.0"
edition = "2021"
license = "MIT"
repository = "https://github.com/gogoout/sshfwd.rs"
authors = ["gogoout"]

[workspace.dependencies]
# Serialization
serde = "1"
serde_json = "1"

# System
libc = "0.2"

# SSH and async
russh = "0.63"
ssh2-config = "0.8"
tokio = "1"
futures = "0.3"

# TUI
ratatui = "0.30"
crossterm = "0.29"
crossbeam-channel = "0.5"

# Cryptography
sha2 = "0.11"

# Notifications
notify-rust = "4"

# Clipboard
arboard = "3"

# Image encoding
png = "0.18"

# Error handling
thiserror = "2"

# Workspace crates
sshfwd-common = { version = "0.4.0", path = "crates/sshfwd-common" }

# Optimized release profile for the agent binary (small, statically linked)
[profile.release-agent]
inherits = "release"
opt-level = "z"
lto = true
panic = "abort"
strip = true
codegen-units = 1
